// Moved to src/lib/calculators/utils.ts (M1-W, 11 Sep 2026) so the pure
// calculator functions and the iPhone app's parity tooling can import it
// without React. Re-exported here so nothing else changes.
export * from '@/lib/calculators/utils';
